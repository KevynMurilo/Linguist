version: '3.8'

services:
  linguist-whisper:
    image: onerahmet/openai-whisper-asr-webservice:latest
    container_name: linguist-whisper-dev
    ports:
      - "9000:9000"
    environment:
      - ASR_MODEL=small
      - ASR_ENGINE=faster_whisper
    restart: unless-stopped
